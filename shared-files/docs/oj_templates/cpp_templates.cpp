#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main(int argc, char *argv[])
{
	// t: total cases
	int t; 
	cin >> t;
	for (int i = 0; i < t; ++i) {
		// m: test case lines in total
		int m;
		cin >> m;
	}

	return 0;
}
